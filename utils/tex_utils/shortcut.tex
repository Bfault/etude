\RequirePackage{amssymb}
\RequirePackage{mathrsfs}
\RequirePackage{amsmath}
\RequirePackage{stmaryrd}

\def\N{\mathbb{N}}
\def\Z{\mathbb{Z}}
\def\Q{\mathbb{Q}}
\def\R{\mathbb{R}}
\def\C{\mathbb{C}}
\def\K{\mathbb{K}}
\def\k{\Bbbk}
\def\D{\mathcal{D}}
\def\P{\mathcal{P}}

\def\xn#1{(#1_n)_{n \in \N}}

\def\eps{\varepsilon}
\def\Erond{\mathscr{E}}

% \funcdef{name}{domain}{codomain}{variable}{expression}
% name: Name of the function (e.g. f)
% domain: Domain of the function (e.g. \mathbb{R})
% codomain: Codomain of the function (e.g. \mathbb{R})
% variable: Variables of the function (e.g. x)
% expression: Expression of the function (e.g. x^2)
\newcommand{\funcdef}[5]{%
    \begin{aligned}[t]
        #1\colon #2 &\to #3\\
        #4 &\mapsto #5
    \end{aligned}%
}

% \seriedef{term}{start_index}
% term: General term of the series (e.g. u_n)
% start_index: Starting index of the series (e.g. 0)
\newcommand{\seriedef}[2]{%
    \left(\sum\limits_{n=#2}^{+\infty} #1\right)
}

\newcommand{\lt}{\ensuremath <}
\newcommand{\gt}{\ensuremath >}

\def\n{~\par}

\newcommand{\norm}[1]{\left\lVert#1\right\rVert}
\newcommand{\mnorm}[1]{\left|\!\left|\!\left|#1\right|\!\right|\!\right|}